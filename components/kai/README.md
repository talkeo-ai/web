# kai

Kai is a layer, not a screen: it runs across surfaces rather than being one of them. Whatever it
needs on more than one route lives here.

It can write policy — focus, goals, interests. It can never write evidence or a level. That is
guaranteed by the shape of the port in `core/`, not by a prompt.
