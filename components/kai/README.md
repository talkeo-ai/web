# kai

Kai runs across routes rather than being a screen of its own. Whatever it needs on more than one
route lives here.

What it is allowed to write is constrained by the interface in `core/`, not by prompt text.
